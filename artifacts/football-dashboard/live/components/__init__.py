"""Custom Streamlit Components du dashboard football.

Chaque sous-module expose une fonction `render_xxx(...)` qui pose un
composant React custom dans le DOM Streamlit et retourne la valeur
émise par le composant via `Streamlit.setComponentValue()`.

Voir `lineup_pitch.py` pour le composant terrain de prédiction buteurs
(T023, en cours d'implémentation chantier par chantier).
"""
