# Overview

This pnpm workspace monorepo, built with TypeScript, provides a football analytics dashboard and a supporting API. The project focuses on comprehensive data visualization, ELO rankings, match predictions, odds comparison, and World Cup 2026 simulations, targeting the sports analytics and betting insights market.

**Key Capabilities:**
- **Football Analytics Dashboard:** A Streamlit application for detailed analysis, ELO rankings, match predictions, and World Cup 2026 simulations.
- **API Server:** An Express.js backend for data serving and business logic.
- **Data Scraping:** Tools for collecting odds from bookmakers and national squad information.

# User Preferences

I prefer iterative development, focusing on one feature or bug fix at a time. Please ask for confirmation before making significant architectural changes or adding new external dependencies. For any new features, prioritize robust error handling and clear logging. I value detailed explanations of complex logic, especially in prediction models or data processing.

# System Architecture

The project uses a pnpm workspace monorepo for shared code and independent application deployment.

**Core Technologies:**
- **Monorepo Tool:** pnpm workspaces
- **Node.js:** v24
- **TypeScript:** v5.9
- **API Framework:** Express v5
- **Database:** PostgreSQL with Drizzle ORM
- **Validation:** Zod
- **API Codegen:** Orval (from OpenAPI spec)
- **Build Tool:** esbuild
- **Frontend (Dashboard):** Python + Streamlit

**Monorepo Structure:**
- `artifacts/`: Deployable applications (e.g., `api-server`, `football-dashboard`).
- `lib/`: Shared libraries (e.g., `api-spec`, `api-client-react`, `api-zod`, `db`).
- `scripts/`: Utility scripts.

**TypeScript & Composite Projects:**
All packages extend a base `tsconfig.base.json` with `composite: true` for cross-package type-checking.

**UI/UX (Football Analytics Dashboard):**
The Streamlit dashboard offers an interactive interface with sections for match results, team/player data, ELO rankings, match predictions, odds comparisons, and World Cup 2026 simulations. It integrates an AI Assistant (Claude).

**Technical Implementations & Feature Specifications:**

- **API Server (`@workspace/api-server`):** Express.js server with routes, validation via `@workspace/api-zod`, and database interaction via `@workspace/db`.
- **Database Layer (`@workspace/db`):** Drizzle ORM for PostgreSQL schema and connections.
- **API Specification & Codegen (`@workspace/api-spec`):** Defines OpenAPI 3.1 spec and generates React Query hooks, fetch clients, and Zod schemas.
- **Football Analytics Dashboard (`artifacts/football-dashboard`):**
    - **Prediction Models:** Includes a V8-Pin Optimized Model, a Dynamic ELO System with competition-specific K-factors and time decay, and Monte Carlo Simulations for WC 2026.
    - **Data Processing:** Handles historical odds, scraped odds, and ELO computations.
    - **Scraping:** Betclic odds (gRPC-web) and Transfermarkt national squad data.
    - **Garantie 2+ Section:** Calculates G2+ probabilities based on market 1X2 odds, O/U 2.5, and BTTS using a closed-form analytical method (`lambdas_buchdahl`).
    - **Forward Test Live (Top 5):** Real-time pipeline for validating proprietary scorer/assister models.
        - **Player Pool & Stats:** Builds player pools with season stats, caches career stats, and handles transfer overrides.
        - **Prediction Logic:** Predicts scorer/assister odds using a calibrated anti-Poisson model, resolving player positions and minutes, and managing lineup fallbacks. The scorer engine uses goals-per-90 instead of xG.
        - **Probable Lineup (T008):** When BSD has not yet published the official lineup (~1h pre-kickoff), the pool computes a `start_rate` per player (starts/team_matches), and `build_lineup_fallback` selects the top-11 by start_rate (with mandatory GK guaranteed). Each player carries a "shadow odd" `fair_odd_scorer/assist_if_starter` simulating his odds if he were a starter — useful to spot value on presumed substitutes. The UI shows a "Compo prob. confiance X%" badge and a "Cote si tit." column.
        - **Cote 90' théorique (T009):** All scorer/assist fair odds are computed at theoretical 90' game time (`xg_for_90 = xg_calibrated × 90 / minutes_expected`), aligning with the French "garantie buteur" market convention (the bookmaker's bet is validated even if the player only enters as a substitute). The team xG calibration is preserved (`sum xg_calibrated = team_xg`), only the per-player probability is published at 90'-equivalent. This significantly lowers fair odds for presumed substitutes (e.g. a sub at 25' avg minutes has odds ~3.6× lower than the minutes-aware model) and makes the model directly comparable to bookmaker prices. The "Cote si tit." column (T008 shadow odds) is preserved alongside: post-T009, the gap between Cote juste and Cote si tit. is smaller (since the actual sub odd is already inflated to 90'), but still surfaces the share-renormalization effect of promoting one specific sub to starter.
        - **Expected Shots (T010):** Two descriptive columns added to the predictions table: `xT` (expected shots) = `shots_per_90 × minutes_expected / 90` and `xT cad.` (expected shots on target) = `shots_on_target_per_90 × minutes_expected / 90`. Aggregated from BSD per-game `total_shots` / `shots_on_target` fields. Unlike the cote at 90' theoretical, these use real `minutes_expected` since they describe what the player will actually do in the match. Helpful to spot value: a player with high xT but low p% suggests the model under-rates his danger.
        - **Injured player visibility (T011):** Injured/suspended/doubtful players now appear in the prediction table (previously hidden), unchecked by default. They're shown with the corresponding emoji (🤕 / 🟥 / ❓) and with `xg_calibrated = 0` (no impact on team xG normalization). Their `minutes_expected` defaults to `avg_mins_when_starter` (~85 min) so that if the user manually re-enables them (last-minute rumor of recovery), `_recalculate_shares` produces valid odds based on `xg_per_90_used × 85 / 90`. When a BSD-confirmed lineup includes a player previously flagged as injured (e.g. Sorloth listed in starters despite "doubtful" tag), the BSD lineup is now treated as source of truth and the player is force-included with normal odds calculation.
        - **Manual position overrides (T012):** BSD only provides fine positions (ST/RW/CB/AM/…) for ~10–18% of Top 5 players; the rest are tagged with the coarse codes MID/DEF/FWD/GK. To bypass this limitation, the user's proprietary Excel file (`Buteurs_Maison_4.1.xlsx`, sheet `Joueurs`, column `Poste`) is extracted into `live/data/manual_positions.json` (~2 500 Top 5 players) via `live/extract_manual_positions.py`. The French codes are mapped to BSD codes (BU→ST, AT→SS, AD→RW, AG→LW, MOC→AM, MDC→DM, MC→CM, DC→CB, DD→RB, DG→LB, G→GK). At pool build time, `assign_team_ids_via_squads` looks up each BSD player by `(name, team, country)` in the override dict and stores `pool[pid]["manual_position"]`. The `resolve_detailed_position` cascade now uses this override at step 0 (highest priority, before BSD lineup/squad fields). For players absent from the Excel (typically goalkeepers, U21, fringe substitutes), the BSD cascade still applies. Coverage measured at ~73% on PL pool, ~65% on La Liga pool, ~80% on real prediction players (logged in forward log). Star players impacted: Salah MID→**RW**, Haaland FWD→**ST**, Griezmann FWD→**SS**, Bruno Fernandes MID→**AM**, Yamal MID→**RW**, Vinicius FWD→**LW**, Saka MID→**RW**, Bellingham MID→**AM**, Lewandowski FWD→**ST**, Mbeumo confirmed RW.
        - **Result Enrichment:** Retrieves outcomes and updates prediction logs.
        - **UI Streamlit (`live/ui.py`):** Provides detailed match predictions, an edge testing table, and tracking KPIs for forward tests.
        - **StatsHub complementary lineup (T013):** Read-only integration with StatsHub.com (no auth required). The new module `live/statshub_helpers.py` resolves any BSD match to its StatsHub external (Sportradar) id via fuzzy team-name + kickoff-date matching, then fetches the predicted lineup via `/api/event/{externalId}/predicted-teams-lineup`. Results are cached locally (`live/data/statshub_match_cache.json`, TTL 30 min, negative results cached too to avoid retries). In `_render_match_detail`, a new collapsible "Compo prédite (StatsHub)" expander appears below the existing BSD lineup block, displaying the home/away predicted XI side-by-side with shirt numbers and positions. Designed as a strict complement: if StatsHub is unreachable or no match is found, nothing is rendered (zero impact on existing UI). The Buteurs Maison 4.1 model and the BSD-primary pipeline are NOT modified — StatsHub is purely visual additional context.
        - **Cache StatsHub massif Top 5 + UCL/UEL (T-CACHE-A/B/C/D, FAIT 28/04/2026):** Construction d'un cache local hors-ligne des stats StatsHub pour tous les joueurs des squads des 5 grands championnats + UCL + UEL, exploitable par `predict_today_v2` et le futur volet Passeur sans aucun rappel réseau. **Garde-fou strict** : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py` et le modèle Buteurs Maison 4.1 ne sont **PAS** touchés. Trois nouveaux modules : (1) `live/statshub_helpers.py` étendu avec `find_external_id_for_player(name, country=None) → dict | None` (parse `/api/search?q=` champ `players[]`, fuzzy match + filtre pays), `country_to_statshub_slug` mapping et `fetch_player_performance(ext_id, limit=200)` (lecture brute du payload). (2) `live/build_statshub_cache.py` builder à 2 phases : Phase 1 sweep `search` parallèle 10 workers résoud nom→externalId, Phase 2 sweep `/performance?limit=200` en 5 workers (timeout 25s pour gros payloads >100KB), idempotent TTL 7j, sauve tous les 20 joueurs, paramètre `--time-budget` pour batches courts (60-75s) résilients aux interruptions du shell. (3) `live/statshub_cache.py` reader pure-disque sans réseau exposant `get_external_id_for_bsd_player`, `get_index_entry`, `get_performance_raw`, `has_performance`, `aggregate_performance(ext, utid, season)`, `get_history_by_competition(ext)`, `index_summary`, `performance_count_on_disk`. Les agrégats réutilisent `_aggregate_performance_payload` de `player_stats_router.py` (corrigé pour le format réel `{data:[{player_statistics_event, events:{uniqueTournamentId}, homeTeam, awayTeam}]}` couramment retourné par StatsHub en avril 2026). **Résultats finaux** : 4 701 joueurs uniques après dédup (5 701 brut), 4 117 résolus (87.6%), 4 107 fichiers performance en cache (100% des résolus, 10 collisions d'externalId = même joueur indexé via plusieurs squads), 450 MB de payloads bruts + 2.2 MB d'index. Lecture cold disk ~2 ms / joueur, agrégation par compétition <50 ms, agrégation toutes compétitions ~500 ms worst case (Kane 195 matchs cumulés). Validé sur Kane (ext 108579) : Bundesliga 91M/95G/76xG, UCL 36M/31G/24xG. Causes des 584 miss : noms tronqués dans les squads BSD (« Lewandowski » seul), joueurs UEL de petites ligues nordiques/slaves faiblement indexés, homonymes sans contexte pays. UECL squad absente (`conference_league_squads.json` à ajouter plus tard pour étendre). Aucun rate-limit StatsHub observé sur 5 000+ requêtes. Layout : `live/data/statshub_players_index.json` (mapping bsd_id → entry) + `live/data/statshub_performance/{ext_id}.json` (raw dump). Rapport complet : `.local/notes/statshub-cache-build-2026-04-28.md`.
        - **Boards de composition manuelle interactifs sur Prédiction Buteurs (T023, EN COURS — chantiers 1-2/5 livrés 29/04/2026 : bridge Custom Component opérationnel + composant terrain interactif rendu sur fixture Atlético-Arsenal UCL) :** **Chantier 2 livré (29/04 PM)** : 6 fichiers TS (`src/types.ts`, `src/formations.ts`, `src/components/PlayerDot.tsx`, `Pitch.tsx`, `Bench.tsx`, `StatsPanel.tsx`) + `src/App.tsx` orchestrateur réécrit. Fixture `src/fixtures/atletico_arsenal.json` généré depuis `forward_log.jsonl` (28 joueurs Atlético + 26 joueurs Arsenal avec cotes Buteur/Passeur fair+marché+edge, xG/xA/xT/xTC, minutes, start_rate, availability, injury_type). 6 schémas implémentés (`4-3-3 / 4-2-3-1 / 4-4-2 / 3-5-2 / 3-4-3 / 5-3-2`) avec coordonnées (x,y) 0-100, helpers `autoAssign(roster, formation)` + `detectFormation(roster)` (compte starters par bucket, fallback intelligent). Toggle home/away + dropdown schéma (badge OVERRIDE jaune si différent de la référence détectée) + boutons Reset (restaure la compo BSD) / Save (envoie le `SaveDelta {side, formation, starters_pids[], minutes_overrides{}}` à Python via `setComponentValue`) + banner "modifications non sauvegardées" en bas. Pastilles cliquables avec halo cyan pulsant + cote Buteur jaune sous le nom + badge statut top-right (?/+/■ selon `availability`). Panneau droit conditionnel (slide-in 360 px) avec header gradient cyan, sections "Marchés bookmakers" (Cote Buteur+Passeur fair vs Betclic + edge%) et "Stats attendues match" (xG/xA/xT/xTC + per_90), slider minutes 0-95 avec restore default. Banc en bas en pills cliquables. Bundle 420 KB gzip 117 KB, TS strict passe sans erreur. Rendu validé visuellement sur Streamlit `?page=predictions_buteurs` : terrain 4-2-3-1 (référence détectée pour Atlético) avec Oblak en gardien, Griezmann pointe (cote 3.35), Almada milieu offensif (9.09), 4 défenseurs alignés, banc 17 joueurs visible. Helper Python `live/components/lineup_pitch.py` inchangé (passe juste `home_team/away_team/kickoff/league` en args, le composant retombe sur le fixture pour le roster). **Chantier 3 à venir** : remplacer le fixture hardcodé par les vraies données du forward log de l'event courant (chargement `pd.read_json` sur `forward_log.jsonl` filtré par `event_id` côté Python, sérialisation en JSON injectée dans `event_data["roster"]`). **Chantiers 4-5** : persistance JSON des `SaveDelta` (chantier 4) puis affichage de l'état "compo modifiée par l'utilisateur" sur la liste des matchs en page Prédiction Buteurs (chantier 5). _(Note initiale archivée ci-dessous.)_ Suite au constat que `is_player_unavailable` (predict_today.py:268) marque comme indisponibles tous les joueurs dont `availability != "available"` — et donc exclut à tort les `doubtful` (Vitinha, Doué, Fabián Ruiz dans le test PSG-Bayern UCL → 9 joueurs PSG sortis du calcul, Marquinhos isolé), l'utilisateur a explicitement refusé de toucher au moteur Buteurs Maison 4.1 (T022 abandonné : option A `is_player_unavailable` rejetée). À la place, ajouter dans CHAQUE fiche match un **board visuel de composition manuelle**, purement décoratif côté pipeline (zéro recalcul moteur, zéro impact xG, zéro modif `predict_today.py`). **Phase 1 — Maquettes Canvas (FAITE)** : 3 variantes UI prototypées dans `artifacts/mockup-sandbox/src/components/mockups/lineup-builder/` (`PitchClassic.tsx` terrain vert traditionnel pastilles bleues + initiales, `PitchDark.tsx` tableau tactique sombre cartes rouges + numéros maillot, `PitchCompact.tsx` mini-schéma + liste verticale par ligne). Une 4ème maquette de synthèse `PitchClassicPro.tsx` représente la combinaison retenue. Iframes canvas : `lineup-pitch-classic` / `lineup-pitch-dark` / `lineup-pitch-compact` (rangée du haut à y=-540) + `lineup-pitch-classic-pro` (rangée du bas à y=450). **Décision design finale (option d)** : terrain Classique pour l'esthétique + interaction du Compact pour l'efficacité. Quand l'utilisateur clique sur un joueur, son halo passe en cyan pulsant et un **panneau s'ouvre à droite** affichant 3 sections : (1) header joueur (numéro, poste, badge statut "DOUBTFUL/INJURED/SUSPENDED" + raison), (2) **Marchés bookmakers** : Cote Buteur (fair vs marché + edge%) et Cote Passeur (idem), (3) **Stats attendues match** : xG (Buts attendus), xA (Passes décisives attendues), **xT = Expected Tirs**, **xTC = Expected Tirs Cadrés** — chacune avec valeur match (per_90 × minutes_attendues) et per_90 en sous-titre, (4) liste **"Remplacer par..."** : roster équipe trié par minutes saison avec badges statut. **6 stats validées, toutes disponibles immédiatement** : odds via cascade `live/odds_router.py` (BSD inline → BSD compareOdds → TheOddsAPI → Betclic), edge calculé contre fair odd Buteurs Maison 4.1 ; xG/xA/xT/xTC via StatsHub `/api/player/{id}/performance` (`xg_per_90`, `xa_per_90`, `shots_per_90`, `shots_on_target_per_90` agrégés sur la saison du championnat domestique, multipliés par les minutes attendues du match). **6 schémas tactiques supportés** : 4-3-3 (défaut), 4-2-3-1, 4-4-2, 3-5-2, 3-4-3, 5-3-2. **Persistance** : `live/data/manual_lineups/event_<id>.json`, écrasé quand StatsHub fournit la lineup officielle ~1h avant kickoff (mode V1 — V2 ferait fusion intelligente préservant les overrides). **PAS de banc, PAS de recalcul moteur** — le board est purement visuel, le pipeline V2 multi-ligues continue de tourner inchangé. **Décision technique : Option A — Custom Streamlit Component React** (Option B native Streamlit rejetée car perdait halo cyan animé et gradient terrain). **Découpage en 7 chantiers (~2 jours version complète)** : (a) plomberie React ↔ Python via postMessage (~3-4h), (b) brancher les 6 stats sur les vraies sources avec orchestration cache pour 22 joueurs × 30 matchs/jour (~4-5h), (c) persistance JSON avec fusion bidirectionnelle StatsHub/manuel (~2h), (d) roster équipe + tri remplaçants via BSD `getTeamSquad` + StatsHub (~2h), (e) layout des 6 schémas tactiques (~1-2h), (f) bouton "Remplissage auto" (~2h), (g) tests E2E + intégration dans fiches match existantes (~3h). **Version allégée possible (~1 jour)** en sacrifiant : pas d'auto-fill au départ (gain 2h), pas de fusion intelligente avec lineup officielle StatsHub — simple écrasement quand officielle arrive (gain 2h). Ces deux raccourcis sont rattrapables en V2. **Garde-fous stricts** : `predict_today.py`, `predict_today_v2.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement INTOUCHABLES — aucun de ces fichiers ne sera modifié par T023.
        - **Activation des compositions confirmées BSD via mismatch de clés `players` ↔ `starters` + fallback `/matches/` (T025, FAIT 28/04/2026):** Fix du bug où la bandeau **« ⏳ Compositions non encore confirmées par BSD »** restait affichée même quand les compos officielles étaient publiées (vu sur PSG-Bayern UCL à 25 min du kick-off, alors que les 11 titulaires PSG 4-3-3 et Bayern 4-2-3-1 étaient bien dans BSD). **Cause racine** : `bsd_helpers.get_event_detail` appelle `/api/events/{id}/` qui renvoie les titulaires sous la clé `lineups.{home,away}.players`, mais `predict_today.get_lineup_for_event` (V1, intouchable) lit `side_block.get("starters") or side_block.get("starting")` — pas `players`. Donc la compo officielle était dans le payload depuis le début, simplement sous une clé que le V1 ne savait pas lire → fallback `build_lineup_fallback` (top-11 par start_rate saison) au lieu des vrais titulaires. Effet collatéral : `home_lineup_confirmed=False`/`away_lineup_confirmed=False` dans le forward log → bandeau UI "⏳ non confirmées", `lineup_confidence` calculé au lieu d'être confirmé, minutes_expected non calibrées sur la vraie compo. **Fix dans `live/bsd_helpers.py`** : (1) après le call `/events/{id}/`, copie systématique `lineups.{side}.players → lineups.{side}.starters` quand non vide (rename sans transformation, format de chaque joueur `{player_id, position}` déjà compatible avec V1 — pos `G/D/M/F` honoré par `_resolve_minutes`). (2) Si `starters` reste vide pour au moins un côté, fallback HTTP supplémentaire sur `/api/matches/{id}/` qui sert un payload temps-réel à jour (cas où `/events/` est en retard de quelques minutes sur la propagation BSD). Préserve `substitutes` tel que renvoyé par BSD (souvent vide avant kick-off : le banc complet n'est annoncé qu'au coup d'envoi, le V1 fait alors `build_lineup_fallback` uniquement pour le banc tout en gardant les 11 titulaires officiels). **Tests live** : sur PSG-Bayern (event 202968), avant fix `home_lineup_confirmed=False` + Kane fair_odd_scorer=2.10 (λ Bayern 1.73 distribué sur ~25 joueurs présumés). Après fix : `home_lineup_confirmed=True`, `away_lineup_confirmed=True`, `lineup_confirmed=True`, 22 starters explicites (11+11) + 14 joueurs absents, **Kane fair_odd_scorer=2.05** (recalibré sur les 11 vrais titulaires Bayern), `minutes_expected=83.1`. **Garde-fous T017 préservés** : `predict_today.py`, `predict_today_v2.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement INTOUCHÉS — fix entièrement contenu dans `bsd_helpers.get_event_detail`, transparent pour tous les callers (V1, V2, UI). Pipeline V2 multi-ligues continue de tourner inchangé. Coût : 1 call HTTP additionnel par event uniquement quand `/events/` n'a pas encore les compos.
        - **Cote juste UI = cote moteur Buteurs Maison 4.1 par défaut, recalcul live uniquement en mode what-if (T024, FAIT 28/04/2026):** Fix du bug où la colonne **Cote juste** de la page **Prédiction Buteurs** affichait des cotes **systématiquement décalées** (~10% trop basses pour les titulaires) par rapport au moteur loggué dans `forward_log.jsonl`. Exemple PSG-Bayern UCL test live : forward_log Kane = **2.10** (cote moteur 4.1, λ Bayern 1.73 distribuée sur 25 joueurs du pool), UI affichait Kane = **1.90** (λ 1.73 redistribuée sur seulement 11 présumés titulaires). Toutes les autres cotes affichées étaient pareillement gonflées : Kvaratskhelia 2.78→2.70, Dembélé 2.96→2.87, etc. **Cause racine** : `_render_predictions_editor` appelait `_recalculate_shares(sub, included_pids)` à CHAQUE chargement, et `_initial_inclusion_state` ne coche par défaut que `is_presumed_starter` (top-11 start_rate par équipe) — donc même sans clic utilisateur, la redistribution se faisait sur 11 starters au lieu des ~25 joueurs du pool moteur. Le bandeau "Cote juste recalculée live" ne s'affichait que si `n_user_changes > 0`, donc l'utilisateur n'avait aucun signal que les cotes affichées différaient du log. **Fix dans `live/ui.py`** (Option A choisie sur 3 proposées) : (1) Calcul de `n_user_changes` REMONTÉ avant le branchement de calcul. (2) Branchement conditionnel : si `n_user_changes == 0` → `recalc = sub.copy()` (cotes telles que loggées par le moteur), sinon → `recalc = _recalculate_shares(sub, included_pids)` (mode what-if explicite). (3) Suppression du calcul dupliqué de `n_user_changes` plus bas, refactor du bandeau `st.info` pour réutiliser la même variable et préciser "(mode what-if)" dans le texte. **Garde-fous T017 préservés** : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement intouchés ; aucune modification du forward log ni du pipeline V2 ; `_recalculate_shares` lui-même inchangé (toujours utilisé en mode what-if). Tests directs : reproduction du forward_log Kane=2.10 confirmée via `recalc=sub.copy()` au premier chargement (default_state=22/51 cochés, n_user_changes=0). Test what-if : décochage de Manuel Neuer (1 inclusion modifiée) → bascule sur `_recalculate_shares` qui retourne Kane=1.90, bandeau what-if s'affiche correctement. Code review architect : aucun problème severity HIGH, structure des colonnes préservée, logique tracking forward bet (`add_forward_bet` ligne ~1310) compatible (sauve la cote affichée à l'écran, ce qui correspond au signal d'achat de l'utilisateur).
        - **Bouton "Rafraîchir prédictions" branché sur le pipeline V2 multi-ligues + bypass cache odds (T021):** Fix du bug où cliquer sur **🔄 Rafraîchir prédictions** dans la page **Prédiction Buteurs** ne mettait PAS à jour les xG des matchs hors Top 5 (typiquement PSG-Bayern UCL : cotes Pinnacle changées mais xG figés). **Cause racine** : le bouton appelait `live/predict_today.py` (v1) qui n'itère que sur `TOP5_LEAGUES` (Premier League / La Liga / Bundesliga / Serie A / Ligue 1), donc les matchs UCL/UEL/UECL et ~25 ligues secondaires n'étaient jamais re-pricés — leurs prédictions restaient figées sur le dernier passage de `predict_today_v2.py` (lancé manuellement ou via cron, pas exposé en UI). En plus, même quand v2 tournait, le router odds (`live/odds_router.py`) cache les fetchs pendant 30 min sur disque (`live/data/odds_router_cache.json`), donc rafraîchir 2 fois en 30 min ne ramenait pas de cotes fraîches. **Fix dans `live/ui.py`** : (1) `_run_predict_today(args_extra)` pointe désormais sur `predict_today_v2.py` au lieu de v1 — v2 réutilise STRICTEMENT le moteur Buteurs Maison 4.1 via `import live.predict_today as pt` (zéro modification), étend in-memory `pt.TOP5_LEAGUES` avec toutes les ligues actives du registre via `_extend_top5_with_registry()`, et couvre par défaut les 29 ligues Tier 1 (5 Top + UCL + UEL + 22 secondaires) avec `--leagues all`. Timeout passé de 5 → 10 min pour absorber les pools jamais buildés. (2) Nouvelle fonction `_invalidate_odds_router_cache()` qui supprime `live/data/odds_router_cache.json` avant chaque clic explicite sur le bouton, retourne le nombre d'entrées purgées (visible dans le spinner) et survit aux corruptions / fichier absent. (3) Le tooltip du bouton + le message de spinner expliquent maintenant explicitement la portée : "Re-fetch des cotes (Pinnacle/Bet365/Betclic) + re-calcul des xG sur Top 5 + UCL/UEL + ~25 ligues secondaires. Le cache odds 30 min est purgé pour garantir des cotes fraîches." Le message de succès liste les 3 effets : "squads BSD + cotes fraîches + xG re-calculés". Coût d'1 clic ≈ 2-5 min (pipeline V2 sur ~30 ligues × n matchs J+0..J+2) + ~30-50 appels TheOddsAPI fresh post-purge cache. **Garde-fous T017 préservés** : `predict_today.py` (v1) reste strictement intouché et continue à fonctionner pour les usages CLI Top-5-only, `g2_engine.py`, `betclic_scraper.py` et le modèle Buteurs Maison 4.1 ne sont pas modifiés. Tests : `_invalidate_odds_router_cache()` purge bien 36 entrées sur cache existant, retourne 0 sur cache absent ; `predict_today_v2.py --help` OK ; page Prédiction Buteurs s'affiche sans erreur après restart.
        - **Score le plus probable comme xG/xGA pour le départage de poule (T020):** Le simulateur de Coupe du Monde utilise désormais le **score le plus probable issu des cotes bookmakers** (Pinnacle via TheOddsAPI) comme buts marqués/encaissés des poules pour le départage FIFA art. 13 step 2 (d/e diff buts globale + buts marqués), en remplacement des buts simulés Poisson tirés au hasard. **Architecture en 3 couches** dans `wc_simulator.py` : (1) `derive_lambdas_from_elo(elo_h, elo_a) → (λ_h, λ_a)` : extraction déterministe des paramètres Poisson cappés [0.3, 4.0] depuis l'Elo (factorise la formule interne historique de `simulate_match_goals` sans changer son comportement). (2) `most_likely_score(λ_h, λ_a, max_goals=8) → (h*, a*)` : maximise P(H=i, A=j) = pmf(i, λh) × pmf(j, λa) sur la grille [0,8]² (Poisson indépendant). (3) `simulate_tournament` accepte un nouveau paramètre `params["expected_scores"]` = dict `{(home_code, away_code): (h*, a*)}` pré-calculé une seule fois en dehors de la simulation. Pour chaque match de poule : l'**issue (W/D/L) reste stochastique** via `simulate_match_goals` Elo (variance des points préservée → toujours une vraie distribution Monte-Carlo), mais les **gf/ga additionnés au standings ET au h2h_log** utilisent `(h*, a*)` issus des cotes (constants à travers les 10000 sims pour un même match). Fallback automatique pour les matchs absents de Pinnacle : `most_likely_score(derive_lambdas_from_elo(elo_h, elo_a))` — donc même les matchs hors Pinnacle ont un score "attendu" déterministe (mais Elo-derived, moins informatif que le marché). **Côté `app.py`** : nouvelle fonction `_build_expected_scores()` qui appelle `_cached_pinnacle_data()` puis `g2_engine.lambdas_buchdahl(oh, od, oa, ou25_under, ou25_over)` puis `most_likely_score(lt, lo)` pour chaque match Pinnacle dispo (~30 matchs sur 72). Le dict est passé à `_cached_simulation(n, expected_scores_key)` avec une clé `tuple(sorted(scores.items()))` hashable pour le cache Streamlit (mêmes cotes → même clé → cache hit). **UI** : nouvelle colonne "Score" en première position du tableau de l'onglet "Probabilités 1X2 + xG & O/U" (page CDM 2026 → onglet matchs), affiche le score le plus probable au format `1-1` / `2-0` / `0-0` à côté de la valeur xG (les λ continus déjà affichés). **Trade-off documenté** : avec `expected_scores`, le départage devient déterministe (constant entre les sims pour un match donné) → moins de variance dans l'ordre des classements quand 2 équipes finissent à pts égaux, mais le signal est cohérent avec l'opinion marché pré-tournoi (information bookmaker) plutôt que le bruit Poisson Elo. La variance globale du tournoi (qui gagne, qui passe en R32, etc.) reste forte car les pts agrégés sur 3 matchs sont toujours stochastiques. Tests : `most_likely_score` valide sur cas typiques (1.5/1.2 → 1-1, 2.5/0.8 → 2-0), conservation parfaite des probas winner/runner_up/bronze = 100% chacune sur 200 sims avec et sans `expected_scores`, rétrocompat `params=None` validée. Garde-fous T017 préservés : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement intouchés.
        - **Conformité officielle FIFA WC 2026 articles 12-13 (T019):** Mise en conformité du simulateur Coupe du Monde avec le règlement officiel FIFA (PDF `.local/refs/FWC2026_regulations_EN.pdf`). 3 changements dans `wc_simulator.py` + 4 dans `app.py`. (1) **Match pour la 3e place (FIFA art. 12.10) IMPLÉMENTÉ** : `simulate_tournament` joue désormais le bronze match entre les 2 perdants des SF via `simulate_knockout_match`, marque tracker `bronze` pour le vainqueur du match 3e place et `runner_up` pour le perdant de la finale. Avant ce patch, ces deux événements étaient ignorés (les `sf_losers` étaient capturés mais jamais utilisés). (2) **Départage intra-groupe FIFA art. 13 STRICT** : refactor complet de `_rank_group(standings)` → `_rank_group(standings, h2h_log, elo_map)`. Avant le patch, le tri appliquait directement `(-pts, -diff_globale, -gf_global)` — une simplification qui ignorait le critère officiel FIFA "matches between the teams concerned" (H2H sur sous-ensemble d'équipes ex æquo). Désormais : Step 1 (a/b/c) FIFA appliqué via fonction récursive `_rank_tied_subgroup` qui calcule pts/diff/buts H2H sur le sous-ensemble courant uniquement, puis re-groupe par triplet H2H identique : (i) singletons → ordre figé, (ii) sous-sous-groupe strictement plus petit → récursion (re-application du step 1 sur les matchs entre seulement ces équipes), (iii) sous-sous-groupe = sous-groupe entier (pas de progrès) → fallback step 2 globaux (d/e) puis step 3 Elo (proxy ranking FIFA). Le step 2 (f) score conduite cartons est OMIS (cartons non modélisés en Monte-Carlo, l'Elo le remplace pragmatiquement) et le step 3 (g/h) ranking FIFA récent puis précédent est REMPLACÉ par l'Elo pré-tournoi (cohérent avec `_pick_best_thirds`). Pour transmettre les données H2H, `simulate_tournament` construit désormais un `h2h_log` (defaultdict imbriqué `pts/gf/ga` par paire d'équipes) au fil des 6 matchs de poule et le passe à `_rank_group`. **Mesure d'impact** : la version sans récursion stricte (commit précédent) et la version récursive donnent des classements identiques dans ~99.5% des poules simulées Monte-Carlo (Poisson goals). Pour ~0.5% des poules avec ex æquo profond, la récursion produit un ordre différent — propage à ~5% des tournois 10000-simulés une médaille bronze ou un dernier R32 différent. **Conformité atteinte** : équivalent strict FIFA art. 13 modulo le critère cartons (dérogation documentée). (3) **Output `run_simulation` enrichi** : ajout de 3 nouveaux champs additifs `p_runner_up` (% de finir 2e), `p_bronze` (% de gagner le match 3e place), `p_podium = p_winner + p_runner_up + p_bronze` (% médaille). Compteurs agrégés cohérents : sommes `p_winner`, `p_runner_up`, `p_bronze` chacune = 100% (conservation parfaite vérifiée sur 200 sims). Champ `opponents.bronze` également exposé pour adversaires probables au match 3e place. (4) **UI `app.py`** : tableau principal CDM 2026 (vue "Toutes nations") enrichi avec colonnes "🥈 2e", "🥉 3e", "Podium" en plus du "🏆 Titre" existant. Onglet bracket adversaires probables : ajout du stage "Match 3e place" avec `p_reach = max(0, p_sf - p_final)` (atteint ce match si on perd la SF). Champs existants (`p_r32`...`p_winner`) préservés à 100%, accès aux nouveaux via `.get(key, 0)` pour rétrocompatibilité. Tests E2E : 4 cas couverts (cycle parfait départagé par Elo, H2H non-circulaire respecté, 3 médailles distinctes attribuées, conservation probas 200 sims). Garde-fous T017 préservés : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement intouchés. Architect review intégrée : signalait que tuple sort ≠ FIFA séquentiel strict avec ~5% de divergence sur tournois → récursion stricte implémentée en réponse, gap fermé.
        - **Bet365 prioritaire dans odds_router + règle FIFA des meilleurs 3èmes (T018):** Deux patches indépendants ciblés sur la précision : (1) **Bet365 prioritaire dans `live/odds_router.py`** — ajout de `_bet365_odd(market, selection_keys)` qui parcourt `market[sk]["bookmakers"]` et préfère les bookmakers dont le nom commence par `"Bet365"` (couvre toutes les variantes régionales : Bet365 Spain, Italy, Latam, IA, etc., toutes vérifiées présentes dans le catalogue BSD `listBookmakers`). `_try_bsd_compare_via_helpers` utilise désormais une fonction interne `_pick(market, keys)` qui essaie Bet365 puis fallback sur le `best_odds` historique tous bookmakers, en comptant les sources pour produire 3 nouveaux labels `source` : `bsd_bet365` (100% Bet365 → cohérence cross-market garantie, λ équipe le plus précis car les 3 marchés racontent la même histoire), `bsd_mix` (Bet365 sur certains marchés + best_odds sur d'autres), `bsd` (aucun marché Bet365, équivalent legacy). **Limite constatée en production** : 100% des prédictions actuelles passent par `bsd_event_inline` (cotes du payload `getMatchDetail`), donc le patch est dormant tant que la cascade ne descend pas au niveau `compareOdds`. Sur les Top 5 + UCL, l'endpoint `compareOdds` REST renvoie souvent 404 / "No odds found" → l'effet Bet365 ne sera observable que sur les championnats que `compareOdds` couvre vraiment (à explorer). **Extension côté TheOddsAPI (avril 2026, après retour du quota mensuel)** : la même stratégie Bet365-prioritaire a été ajoutée à la branche `_try_theoddsapi` du router. Les helpers internes `_is_bet365`, `_extract_1x2`, `_extract_ou25` permettent un double passage : Bet365 d'abord sur 1X2 puis sur O/U 2.5, fallback sur le premier bookmaker EU disponible quand Bet365 manque. Trois nouveaux labels `source` cohérents avec la branche BSD : `theoddsapi_bet365` (Bet365 sur les 2 marchés), `theoddsapi_mix` (mix Bet365 + autre), `theoddsapi` (legacy, aucun Bet365). Test réel sur Premier League : pour les matchs où la région EU de TheOddsAPI ne propose pas Bet365 (Bet365 historiquement plutôt UK/AU), le fallback gracieux donne `source=theoddsapi` sans rien casser. Pour activer Bet365 effectivement sur ces matchs, il faudrait élargir la requête à `regions=eu,uk` (coût en credits ×2) — non fait par défaut. (2) **Règle FIFA des meilleurs 3èmes dans `wc_simulator._pick_best_thirds()`** — extension de la sort_key de 3 à 5 critères : pts → diff buts → buts marqués → **Elo (proxy ranking pré-tournoi FIFA, plus fort gagne)** → **tirage au sort**. Le critère officiel "fair-play" (cartons) est volontairement omis car la simulation Monte-Carlo ne modélise pas les cartons ; l'Elo le remplace pragmatiquement (modèle, NON conforme strict FIFA — documenté). Le tirage utilise `random.Random(sim_seed)` si une seed est fournie (reproductible pour debug/tests) sinon `random.Random()` non seedé (vrai aléatoire = pas de biais Monte-Carlo entre simulations). Les `draw_tokens` sont régénérés à chaque appel donc indépendants entre simulations même quand `sim_seed=None`. Tests : 20 runs sans seed → 6 ordres différents quand 3 troisièmes égaux sur tous critères Elo inclus = vrai aléatoire ; 5 runs avec seed=42 → 1 seul ordre = reproductible. Aucune modification au-delà de la sort_key et de sa signature ; `simulate_tournament` lit `sim_seed` depuis `params` et le transmet. Garde-fous T017 préservés : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement intouchés.
        - **Pipeline générique multi-championnats + nav Flashscore (T017):** Extension du forward-test au-delà du Top 5 pour couvrir les coupes UEFA (UCL/UEL/UECL) et ~25 ligues secondaires (Eredivisie, Pro League, Süper Lig, Saudi PL, MLS, Liga MX, Brasileirão, etc.). **Garde-fou strict** : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py` et le modèle Buteurs Maison 4.1 ne sont **PAS** touchés ; tout est ajouté en parallèle. Quatre nouveaux modules : (1) `live/leagues_config.py` — registre central de 30 compétitions avec `LeagueConfig(slug, name, region, flag, bsd_id, statshub_utid, theoddsapi_key, betclic_slug, country_iso, tier)` et helpers `get_active_leagues / get_by_slug / group_by_region / league_labels_dict` ; 6 régions : `uefa / top5 / europe_other / americas / asia_middle_east / africa`. (2) `live/odds_router.py` — cascade BSD inline → BSD compareOdds REST → TheOddsAPI → Betclic, cache disque 30 min, sortie normalisée `{1x2, ou25, btts, source, mode}` + helper `to_predict_today_format()` drop-in compatible avec `lambdas_buchdahl`. (3) `live/player_stats_router.py` — agrégation StatsHub `/api/player/{id}/performance` filtrée par tournamentId domestique avec calcul `xg_per_90/xa_per_90/npxg_per_90/minutes_total/goals_total/samples`, cache 24 h, fallback Excel Buteurs_Maison_4.1 si StatsHub vide. (4) `live/predict_today_v2.py` — orchestrateur générique qui importe `pt.load_pool / pt.get_lineup_for_event / pt.compute_lineup_confidence / pt.find_betclic_match / pt.find_betclic_player_odd / pt.resolve_detailed_position / pt.scrape_betclic_leagues / pt.load_existing_log` (réutilisation totale du moteur 4.1) et étend in-memory `pt.TOP5_LEAGUES / pt.LEAGUE_TO_COUNTRY / pt.LEAGUE_TO_BETCLIC_KEY` via `_extend_top5_with_registry()` (mutation locale au process, jamais persistée). `predict_one_event_v2` injecte le router odds en cascade et ajoute trois champs traçabilité au forward log : `league_region`, `odds_source`, `odds_mode`. La logique d'écriture (file lock, dedup, purge orphelins ≥10 fresh players, upsert pré-kickoff, **invariant "never overwrite outcome_scored"**) est strictement reprise de `pt.main`. **UI Flashscore-like** dans `live/ui.py` page `predictions_buteurs` : remplacement du radio horizontal par une **sidebar arborescente** avec un `st.expander` par région (UEFA / Top 5 / Europe autres / Amériques / Asie-Moyen-Orient / Afrique), drapeau ISO + compteur matchs par ligue, deux toggles (`⭐ Top 5 uniquement`, `💰 Edge ≥ 5% uniquement`), bouton reset `🔄 Toutes les compétitions`, sélection persistée via query param `?comp=ligue_slug`. Seules les ligues présentes dans le forward log sont exposées (pas de nav creuse). `LEAGUE_LABELS` est désormais auto-généré depuis le registre via `league_labels_dict()` avec override pour Premier League (drapeau Angleterre vs UK). Aucun changement schema forward log à respecter ; les nouveaux champs sont additifs. **Activation effective (T017 livré) :** patch minimal appliqué à `live/build_player_pool.py` (ajout d'un `_extend_top5_with_registry()` qui mute le dict en mémoire avant la résolution des slugs, comportement legacy `--leagues all` préservé = Top 5 only). 29 pools construits sur 30 ligues du registre (UECL skip car pas de bsd_id), poids cumulé ~150 MB de stats joueur par match (BSD season 2025-2026). Pipeline V2 lancé en batches de 5 ligues sur l'ensemble : ~3700 lignes de prédictions ajoutées au forward log, couvrant UCL/UEL, Championship, Eredivisie, Liga Portugal, Pro League, Süper Lig, Scottish Premiership, Segunda División, Allsvenskan, Ekstraklasa, Greek Super League, Swiss Super League, MLS, Liga MX (Apertura+Clausura), Brasileirão A+B, Copa Libertadores+Sudamericana, Saudi Pro League, J1, K League 1, CAF Champions League. Cascade odds testée en réel : 1 match UEL routé via TheOddsAPI (mode B) après échec BSD inline, fallback gracieux sur les events sans odds (skip avec log explicite, jamais de prédiction sur des cotes inventées).
        - **Filter par compétition sur Prédiction Buteurs (T016):** Au-dessus du sélecteur de match, un radio horizontal `🏆 Filtrer par compétition` liste « Toutes » + chaque ligue présente dans le forward log avec son compteur de matchs prévus (ex : `🇩🇪 Bundesliga (6) · 🇫🇷 Ligue 1 (4) · …`). Persistance via query param `?comp=ligue_1` pour URL bookmarkable / partageable. Si une seule ligue est présente le filtre est masqué (inutile). Si la ligue sélectionnée n'a aucun match futur, message d'invitation. Aucun changement de pipeline / de pool.
        - **Forward-test tracking page rebuilt (T015):** The "📈 Tracking Test Edge Buteurs" page is now driven by a dedicated store, separate from the generic bet tracker. New module `live/forward_bets.py` exposes `add_forward_bet / load_forward_bets / update_forward_bet_result / delete_forward_bet / clear_all_forward_bets` and persists to `live/data/forward_bets.json` (initialized empty). Schema captures the full snapshot at bet time: `id, added_at, event_id, match, league_slug/name, kickoff, player_id/name, market (Buteur|Passeur), p_model, fair_odd, betclic_odd, edge_pct, stake, result (pending|won|lost|void), profit_units`. The "💰 Ajouter au tracking" button on "🔮 Prédiction Buteurs" now writes to this store (NOT to `bets_tracker.json`); a lookup in the per-event forward log subset enriches each bet with kickoff/league/p_model. `render_tracking_page` was rewritten from scratch: shows 5 KPIs (n_total/pending, mise totale, P/L réalisé+ROI, hit rate, edge moyen — void excluded from ROI/hit), a sortable detail table (Ajouté · Kickoff · Ligue · Match · Pick · p% · Cote juste · Cote prise · Edge% · Mise · Statut · P/L), an actions panel (manually force status or delete one bet), and a confirmation-gated "🗑 Tout effacer" reset. Auto-resolution: a helper `_auto_resolve_status_from_log` cross-references each pending bet against the enriched forward log via `(event_id, player_id)` and reads `outcome_scored`/`outcome_assisted` based on market — only mutates pending bets (never overrides manual results), defensively guards malformed IDs via `_safe_int`. The Buteurs Maison 4.1 model and BSD pipeline are NOT touched; `bet_tracker.py` and the separate "📊 Suivi des paris" page remain untouched.
        - **Predictions UI cleanup + 1-click tracking (T014):** Three coordinated UI changes on "🔮 Prédiction Buteurs". (1) Removed the legacy "🎯 Test Edge Buteurs" page entirely (sidebar, dispatch, function `render_edges_page` deleted from `live/ui.py`); old query-string shortcut `?page=edges` now redirects to "Prédiction Buteurs" so existing bookmarks keep working. (2) Hidden the "Cote si tit." and "Marché" columns from the interactive `st.data_editor` of `_render_predictions_editor`: "Cote si tit." (and its helper `_cote_si_tit`) is fully removed; "Marché" is preserved internally as `_marche` (hidden via `column_config[..]=None`) so the Buteur/Passeur distinction stays usable in the new tracking block while disappearing from the visible table. (3) Added a "💰 Ajouter au tracking forward test" block below the editor: for any pick with `Edge % > 0` and a Betclic odd, user picks the row, sets a stake, and one click pushes the bet to `bets_tracker.json` via `bet_tracker.add_bet()` (mapping: `odds`=Cote Betclic, `odds_v8`=Cote juste, `notes` includes `ev_id` + edge%). The bet is then visible in the existing "📊 Suivi des paris" page, no schema change required. The Buteurs Maison 4.1 model and BSD pipeline are NOT modified.

# External Dependencies

- **Database:** PostgreSQL
- **Football Data API:** TheStatsAPI
- **Odds API:** The Odds API
- **AI Assistant:** Claude (Anthropic)
- **Data Sources:** Transfermarkt, Betclic, Pinnacle, Betfair Exchange (EU), Unibet FR, PMU FR, AllSportsAPI (for BSD API odds), OPTA Power Ratings.