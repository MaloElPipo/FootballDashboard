# Overview

This pnpm workspace monorepo, built with TypeScript, provides a football analytics dashboard and a supporting API. The project focuses on comprehensive data visualization, ELO rankings, match predictions, odds comparison, and World Cup 2026 simulations, targeting the sports analytics and betting insights market.

**Key Capabilities:**
- **Football Analytics Dashboard:** A Streamlit application for detailed analysis, ELO rankings, match predictions, and World Cup 2026 simulations.
- **API Server:** An Express.js backend for data serving and business logic.
- **Data Scraping:** Tools for collecting odds from bookmakers and national squad information.

# User Preferences

I prefer iterative development, focusing on one feature or bug fix at a time. Please ask for confirmation before making significant architectural changes or adding new external dependencies. For any new features, prioritize robust error handling and clear logging. I value detailed explanations of complex logic, especially in prediction models or data processing.

# System Architecture

The project uses a pnpm workspace monorepo for shared code and independent application deployment.

**Core Technologies:**
- **Monorepo Tool:** pnpm workspaces
- **Node.js:** v24
- **TypeScript:** v5.9
- **API Framework:** Express v5
- **Database:** PostgreSQL with Drizzle ORM
- **Validation:** Zod
- **API Codegen:** Orval (from OpenAPI spec)
- **Build Tool:** esbuild
- **Frontend (Dashboard):** Python + Streamlit

**Monorepo Structure:**
- `artifacts/`: Deployable applications (e.g., `api-server`, `football-dashboard`).
- `lib/`: Shared libraries (e.g., `api-spec`, `api-client-react`, `api-zod`, `db`).
- `scripts/`: Utility scripts.

**TypeScript & Composite Projects:**
All packages extend a base `tsconfig.base.json` with `composite: true` for cross-package type-checking.

**UI/UX (Football Analytics Dashboard):**
The Streamlit dashboard offers an interactive interface with sections for match results, team/player data, ELO rankings, match predictions, odds comparisons, and World Cup 2026 simulations. It integrates an AI Assistant (Claude).

**Technical Implementations & Feature Specifications:**

- **API Server (`@workspace/api-server`):** Express.js server with routes, validation via `@workspace/api-zod`, and database interaction via `@workspace/db`.
- **Database Layer (`@workspace/db`):** Drizzle ORM for PostgreSQL schema and connections.
- **API Specification & Codegen (`@workspace/api-spec`):** Defines OpenAPI 3.1 spec and generates React Query hooks, fetch clients, and Zod schemas.
- **Football Analytics Dashboard (`artifacts/football-dashboard`):**
    - **Prediction Models:** Includes a V8-Pin Optimized Model, a Dynamic ELO System with competition-specific K-factors and time decay, and Monte Carlo Simulations for WC 2026.
    - **Data Processing:** Handles historical odds, scraped odds, and ELO computations.
    - **Scraping:** Betclic odds (gRPC-web) and Transfermarkt national squad data.
    - **Garantie 2+ Section:** Calculates G2+ probabilities based on market 1X2 odds, O/U 2.5, and BTTS using a closed-form analytical method (`lambdas_buchdahl`).
    - **Forward Test Live (Top 5):** Real-time pipeline for validating proprietary scorer/assister models.
        - **Player Pool & Stats:** Builds player pools with season stats, caches career stats, and handles transfer overrides.
        - **Prediction Logic:** Predicts scorer/assister odds using a calibrated anti-Poisson model, resolving player positions and minutes, and managing lineup fallbacks. The scorer engine uses goals-per-90 instead of xG.
        - **Probable Lineup (T008):** When BSD has not yet published the official lineup (~1h pre-kickoff), the pool computes a `start_rate` per player (starts/team_matches), and `build_lineup_fallback` selects the top-11 by start_rate (with mandatory GK guaranteed). Each player carries a "shadow odd" `fair_odd_scorer/assist_if_starter` simulating his odds if he were a starter — useful to spot value on presumed substitutes. The UI shows a "Compo prob. confiance X%" badge and a "Cote si tit." column.
        - **Cote 90' théorique (T009):** All scorer/assist fair odds are computed at theoretical 90' game time (`xg_for_90 = xg_calibrated × 90 / minutes_expected`), aligning with the French "garantie buteur" market convention (the bookmaker's bet is validated even if the player only enters as a substitute). The team xG calibration is preserved (`sum xg_calibrated = team_xg`), only the per-player probability is published at 90'-equivalent. This significantly lowers fair odds for presumed substitutes (e.g. a sub at 25' avg minutes has odds ~3.6× lower than the minutes-aware model) and makes the model directly comparable to bookmaker prices. The "Cote si tit." column (T008 shadow odds) is preserved alongside: post-T009, the gap between Cote juste and Cote si tit. is smaller (since the actual sub odd is already inflated to 90'), but still surfaces the share-renormalization effect of promoting one specific sub to starter.
        - **Expected Shots (T010):** Two descriptive columns added to the predictions table: `xT` (expected shots) = `shots_per_90 × minutes_expected / 90` and `xT cad.` (expected shots on target) = `shots_on_target_per_90 × minutes_expected / 90`. Aggregated from BSD per-game `total_shots` / `shots_on_target` fields. Unlike the cote at 90' theoretical, these use real `minutes_expected` since they describe what the player will actually do in the match. Helpful to spot value: a player with high xT but low p% suggests the model under-rates his danger.
        - **Injured player visibility (T011):** Injured/suspended/doubtful players now appear in the prediction table (previously hidden), unchecked by default. They're shown with the corresponding emoji (🤕 / 🟥 / ❓) and with `xg_calibrated = 0` (no impact on team xG normalization). Their `minutes_expected` defaults to `avg_mins_when_starter` (~85 min) so that if the user manually re-enables them (last-minute rumor of recovery), `_recalculate_shares` produces valid odds based on `xg_per_90_used × 85 / 90`. When a BSD-confirmed lineup includes a player previously flagged as injured (e.g. Sorloth listed in starters despite "doubtful" tag), the BSD lineup is now treated as source of truth and the player is force-included with normal odds calculation.
        - **Manual position overrides (T012):** BSD only provides fine positions (ST/RW/CB/AM/…) for ~10–18% of Top 5 players; the rest are tagged with the coarse codes MID/DEF/FWD/GK. To bypass this limitation, the user's proprietary Excel file (`Buteurs_Maison_4.1.xlsx`, sheet `Joueurs`, column `Poste`) is extracted into `live/data/manual_positions.json` (~2 500 Top 5 players) via `live/extract_manual_positions.py`. The French codes are mapped to BSD codes (BU→ST, AT→SS, AD→RW, AG→LW, MOC→AM, MDC→DM, MC→CM, DC→CB, DD→RB, DG→LB, G→GK). At pool build time, `assign_team_ids_via_squads` looks up each BSD player by `(name, team, country)` in the override dict and stores `pool[pid]["manual_position"]`. The `resolve_detailed_position` cascade now uses this override at step 0 (highest priority, before BSD lineup/squad fields). For players absent from the Excel (typically goalkeepers, U21, fringe substitutes), the BSD cascade still applies. Coverage measured at ~73% on PL pool, ~65% on La Liga pool, ~80% on real prediction players (logged in forward log). Star players impacted: Salah MID→**RW**, Haaland FWD→**ST**, Griezmann FWD→**SS**, Bruno Fernandes MID→**AM**, Yamal MID→**RW**, Vinicius FWD→**LW**, Saka MID→**RW**, Bellingham MID→**AM**, Lewandowski FWD→**ST**, Mbeumo confirmed RW.
        - **Result Enrichment:** Retrieves outcomes and updates prediction logs.
        - **UI Streamlit (`live/ui.py`):** Provides detailed match predictions, an edge testing table, and tracking KPIs for forward tests.
        - **StatsHub complementary lineup (T013):** Read-only integration with StatsHub.com (no auth required). The new module `live/statshub_helpers.py` resolves any BSD match to its StatsHub external (Sportradar) id via fuzzy team-name + kickoff-date matching, then fetches the predicted lineup via `/api/event/{externalId}/predicted-teams-lineup`. Results are cached locally (`live/data/statshub_match_cache.json`, TTL 30 min, negative results cached too to avoid retries). In `_render_match_detail`, a new collapsible "Compo prédite (StatsHub)" expander appears below the existing BSD lineup block, displaying the home/away predicted XI side-by-side with shirt numbers and positions. Designed as a strict complement: if StatsHub is unreachable or no match is found, nothing is rendered (zero impact on existing UI). The Buteurs Maison 4.1 model and the BSD-primary pipeline are NOT modified — StatsHub is purely visual additional context.
        - **Bet365 prioritaire dans odds_router + règle FIFA des meilleurs 3èmes (T018):** Deux patches indépendants ciblés sur la précision : (1) **Bet365 prioritaire dans `live/odds_router.py`** — ajout de `_bet365_odd(market, selection_keys)` qui parcourt `market[sk]["bookmakers"]` et préfère les bookmakers dont le nom commence par `"Bet365"` (couvre toutes les variantes régionales : Bet365 Spain, Italy, Latam, IA, etc., toutes vérifiées présentes dans le catalogue BSD `listBookmakers`). `_try_bsd_compare_via_helpers` utilise désormais une fonction interne `_pick(market, keys)` qui essaie Bet365 puis fallback sur le `best_odds` historique tous bookmakers, en comptant les sources pour produire 3 nouveaux labels `source` : `bsd_bet365` (100% Bet365 → cohérence cross-market garantie, λ équipe le plus précis car les 3 marchés racontent la même histoire), `bsd_mix` (Bet365 sur certains marchés + best_odds sur d'autres), `bsd` (aucun marché Bet365, équivalent legacy). **Limite constatée en production** : 100% des prédictions actuelles passent par `bsd_event_inline` (cotes du payload `getMatchDetail`), donc le patch est dormant tant que la cascade ne descend pas au niveau `compareOdds`. Sur les Top 5 + UCL, l'endpoint `compareOdds` REST renvoie souvent 404 / "No odds found" → l'effet Bet365 ne sera observable que sur les championnats que `compareOdds` couvre vraiment (à explorer). (2) **Règle FIFA des meilleurs 3èmes dans `wc_simulator._pick_best_thirds()`** — extension de la sort_key de 3 à 5 critères : pts → diff buts → buts marqués → **Elo (proxy ranking pré-tournoi FIFA, plus fort gagne)** → **tirage au sort**. Le critère officiel "fair-play" (cartons) est volontairement omis car la simulation Monte-Carlo ne modélise pas les cartons ; l'Elo le remplace pragmatiquement (modèle, NON conforme strict FIFA — documenté). Le tirage utilise `random.Random(sim_seed)` si une seed est fournie (reproductible pour debug/tests) sinon `random.Random()` non seedé (vrai aléatoire = pas de biais Monte-Carlo entre simulations). Les `draw_tokens` sont régénérés à chaque appel donc indépendants entre simulations même quand `sim_seed=None`. Tests : 20 runs sans seed → 6 ordres différents quand 3 troisièmes égaux sur tous critères Elo inclus = vrai aléatoire ; 5 runs avec seed=42 → 1 seul ordre = reproductible. Aucune modification au-delà de la sort_key et de sa signature ; `simulate_tournament` lit `sim_seed` depuis `params` et le transmet. Garde-fous T017 préservés : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py`, modèle Buteurs Maison 4.1 strictement intouchés.
        - **Pipeline générique multi-championnats + nav Flashscore (T017):** Extension du forward-test au-delà du Top 5 pour couvrir les coupes UEFA (UCL/UEL/UECL) et ~25 ligues secondaires (Eredivisie, Pro League, Süper Lig, Saudi PL, MLS, Liga MX, Brasileirão, etc.). **Garde-fou strict** : `predict_today.py`, `g2_engine.py`, `betclic_scraper.py` et le modèle Buteurs Maison 4.1 ne sont **PAS** touchés ; tout est ajouté en parallèle. Quatre nouveaux modules : (1) `live/leagues_config.py` — registre central de 30 compétitions avec `LeagueConfig(slug, name, region, flag, bsd_id, statshub_utid, theoddsapi_key, betclic_slug, country_iso, tier)` et helpers `get_active_leagues / get_by_slug / group_by_region / league_labels_dict` ; 6 régions : `uefa / top5 / europe_other / americas / asia_middle_east / africa`. (2) `live/odds_router.py` — cascade BSD inline → BSD compareOdds REST → TheOddsAPI → Betclic, cache disque 30 min, sortie normalisée `{1x2, ou25, btts, source, mode}` + helper `to_predict_today_format()` drop-in compatible avec `lambdas_buchdahl`. (3) `live/player_stats_router.py` — agrégation StatsHub `/api/player/{id}/performance` filtrée par tournamentId domestique avec calcul `xg_per_90/xa_per_90/npxg_per_90/minutes_total/goals_total/samples`, cache 24 h, fallback Excel Buteurs_Maison_4.1 si StatsHub vide. (4) `live/predict_today_v2.py` — orchestrateur générique qui importe `pt.load_pool / pt.get_lineup_for_event / pt.compute_lineup_confidence / pt.find_betclic_match / pt.find_betclic_player_odd / pt.resolve_detailed_position / pt.scrape_betclic_leagues / pt.load_existing_log` (réutilisation totale du moteur 4.1) et étend in-memory `pt.TOP5_LEAGUES / pt.LEAGUE_TO_COUNTRY / pt.LEAGUE_TO_BETCLIC_KEY` via `_extend_top5_with_registry()` (mutation locale au process, jamais persistée). `predict_one_event_v2` injecte le router odds en cascade et ajoute trois champs traçabilité au forward log : `league_region`, `odds_source`, `odds_mode`. La logique d'écriture (file lock, dedup, purge orphelins ≥10 fresh players, upsert pré-kickoff, **invariant "never overwrite outcome_scored"**) est strictement reprise de `pt.main`. **UI Flashscore-like** dans `live/ui.py` page `predictions_buteurs` : remplacement du radio horizontal par une **sidebar arborescente** avec un `st.expander` par région (UEFA / Top 5 / Europe autres / Amériques / Asie-Moyen-Orient / Afrique), drapeau ISO + compteur matchs par ligue, deux toggles (`⭐ Top 5 uniquement`, `💰 Edge ≥ 5% uniquement`), bouton reset `🔄 Toutes les compétitions`, sélection persistée via query param `?comp=ligue_slug`. Seules les ligues présentes dans le forward log sont exposées (pas de nav creuse). `LEAGUE_LABELS` est désormais auto-généré depuis le registre via `league_labels_dict()` avec override pour Premier League (drapeau Angleterre vs UK). Aucun changement schema forward log à respecter ; les nouveaux champs sont additifs. **Activation effective (T017 livré) :** patch minimal appliqué à `live/build_player_pool.py` (ajout d'un `_extend_top5_with_registry()` qui mute le dict en mémoire avant la résolution des slugs, comportement legacy `--leagues all` préservé = Top 5 only). 29 pools construits sur 30 ligues du registre (UECL skip car pas de bsd_id), poids cumulé ~150 MB de stats joueur par match (BSD season 2025-2026). Pipeline V2 lancé en batches de 5 ligues sur l'ensemble : ~3700 lignes de prédictions ajoutées au forward log, couvrant UCL/UEL, Championship, Eredivisie, Liga Portugal, Pro League, Süper Lig, Scottish Premiership, Segunda División, Allsvenskan, Ekstraklasa, Greek Super League, Swiss Super League, MLS, Liga MX (Apertura+Clausura), Brasileirão A+B, Copa Libertadores+Sudamericana, Saudi Pro League, J1, K League 1, CAF Champions League. Cascade odds testée en réel : 1 match UEL routé via TheOddsAPI (mode B) après échec BSD inline, fallback gracieux sur les events sans odds (skip avec log explicite, jamais de prédiction sur des cotes inventées).
        - **Filter par compétition sur Prédiction Buteurs (T016):** Au-dessus du sélecteur de match, un radio horizontal `🏆 Filtrer par compétition` liste « Toutes » + chaque ligue présente dans le forward log avec son compteur de matchs prévus (ex : `🇩🇪 Bundesliga (6) · 🇫🇷 Ligue 1 (4) · …`). Persistance via query param `?comp=ligue_1` pour URL bookmarkable / partageable. Si une seule ligue est présente le filtre est masqué (inutile). Si la ligue sélectionnée n'a aucun match futur, message d'invitation. Aucun changement de pipeline / de pool.
        - **Forward-test tracking page rebuilt (T015):** The "📈 Tracking Test Edge Buteurs" page is now driven by a dedicated store, separate from the generic bet tracker. New module `live/forward_bets.py` exposes `add_forward_bet / load_forward_bets / update_forward_bet_result / delete_forward_bet / clear_all_forward_bets` and persists to `live/data/forward_bets.json` (initialized empty). Schema captures the full snapshot at bet time: `id, added_at, event_id, match, league_slug/name, kickoff, player_id/name, market (Buteur|Passeur), p_model, fair_odd, betclic_odd, edge_pct, stake, result (pending|won|lost|void), profit_units`. The "💰 Ajouter au tracking" button on "🔮 Prédiction Buteurs" now writes to this store (NOT to `bets_tracker.json`); a lookup in the per-event forward log subset enriches each bet with kickoff/league/p_model. `render_tracking_page` was rewritten from scratch: shows 5 KPIs (n_total/pending, mise totale, P/L réalisé+ROI, hit rate, edge moyen — void excluded from ROI/hit), a sortable detail table (Ajouté · Kickoff · Ligue · Match · Pick · p% · Cote juste · Cote prise · Edge% · Mise · Statut · P/L), an actions panel (manually force status or delete one bet), and a confirmation-gated "🗑 Tout effacer" reset. Auto-resolution: a helper `_auto_resolve_status_from_log` cross-references each pending bet against the enriched forward log via `(event_id, player_id)` and reads `outcome_scored`/`outcome_assisted` based on market — only mutates pending bets (never overrides manual results), defensively guards malformed IDs via `_safe_int`. The Buteurs Maison 4.1 model and BSD pipeline are NOT touched; `bet_tracker.py` and the separate "📊 Suivi des paris" page remain untouched.
        - **Predictions UI cleanup + 1-click tracking (T014):** Three coordinated UI changes on "🔮 Prédiction Buteurs". (1) Removed the legacy "🎯 Test Edge Buteurs" page entirely (sidebar, dispatch, function `render_edges_page` deleted from `live/ui.py`); old query-string shortcut `?page=edges` now redirects to "Prédiction Buteurs" so existing bookmarks keep working. (2) Hidden the "Cote si tit." and "Marché" columns from the interactive `st.data_editor` of `_render_predictions_editor`: "Cote si tit." (and its helper `_cote_si_tit`) is fully removed; "Marché" is preserved internally as `_marche` (hidden via `column_config[..]=None`) so the Buteur/Passeur distinction stays usable in the new tracking block while disappearing from the visible table. (3) Added a "💰 Ajouter au tracking forward test" block below the editor: for any pick with `Edge % > 0` and a Betclic odd, user picks the row, sets a stake, and one click pushes the bet to `bets_tracker.json` via `bet_tracker.add_bet()` (mapping: `odds`=Cote Betclic, `odds_v8`=Cote juste, `notes` includes `ev_id` + edge%). The bet is then visible in the existing "📊 Suivi des paris" page, no schema change required. The Buteurs Maison 4.1 model and BSD pipeline are NOT modified.

# External Dependencies

- **Database:** PostgreSQL
- **Football Data API:** TheStatsAPI
- **Odds API:** The Odds API
- **AI Assistant:** Claude (Anthropic)
- **Data Sources:** Transfermarkt, Betclic, Pinnacle, Betfair Exchange (EU), Unibet FR, PMU FR, AllSportsAPI (for BSD API odds), OPTA Power Ratings.